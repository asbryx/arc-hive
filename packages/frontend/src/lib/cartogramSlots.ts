/**
 * cartogramSlots — wiped. The cartogram is being rebuilt from zero
 * based on Daniel's incoming design guide. This file is an inert
 * stub so any leftover imports compile.
 */

export const VIEWBOX = { w: 1600, h: 800 }
